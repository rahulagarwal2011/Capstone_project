FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml .
COPY src/ src/

RUN pip install --no-cache-dir -e .

ENV RAY_ADDRESS=auto

CMD ["python", "-c", "from reason_reduce.orchestration.scheduler import init_cluster; init_cluster(local=False)"]
